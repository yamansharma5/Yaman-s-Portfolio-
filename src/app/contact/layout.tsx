import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact — Yaman",
  description: "Get in touch with Yaman — Full Stack Developer. Open to opportunities, collaborations, and conversations.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
